import 'package:flutter/material.dart';

class AppTheme {
  AppTheme._();

  static const colorSchemeSeed = Colors.cyan;
  static const horizontalPadding = EdgeInsets.symmetric(horizontal: 16);

  static final borderRadius = BorderRadius.circular(8);
}
